pragma solidity >=0.5.0; //we define the version of solidity progrmming language first

//we now define the contract with contract key word, it is same as file name
contract TodoList {
    uint public taskCount=0; //its a state variable, used to track the state of the contract at any point

}

